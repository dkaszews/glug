{
  "configurations": {
    "Launch glug test": {
      "adapter": "CodeLLDB",
      "configuration": {
        "request": "launch",
        "stopAtEntry": true,
        "program": "${workspaceRoot}/build/${plat:linux}/${arch:x86_64}/${mode:debug}/${exe:glug_test}",
        "args": [ "--gtest_filter=${filter:*}" ],
        "sourceLanguages": [ "cpp" ]
      }
    }
  }
}

