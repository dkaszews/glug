{
  "configurations": {
    "Launch glug test": {
      "adapter": "CodeLLDB",
      "configuration": {
        "request": "launch",
        "stopAtEntry": true,
        "program": "${workspaceRoot}/build/latest/unit_test",
        "args": [ "--gtest_filter=${filter:*}" ],
        "sourceLanguages": [ "cpp" ]
      }
    }
  }
}

